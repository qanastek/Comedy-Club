{% extends 'baseDashboard.html.twig' %}

{% block title %}GalleryEntity index{% endblock %}
{% block pageName %}GalleryEntity index{% endblock %}

{% block body %}
    <h1>GalleryEntity index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Name</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for gallery_entity in gallery_entities %}
            <tr>
                <td>{{ gallery_entity.id }}</td>
                <td>{{ gallery_entity.name }}</td>
                <td>
                    <a href="{{ path('gallery_entity_show', {'id': gallery_entity.id}) }}">show</a>
                    <a href="{{ path('gallery_entity_edit', {'id': gallery_entity.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="3">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('gallery_entity_new') }}">Create new</a>
{% endblock %}
